{
  "connections": [
    {
      "id": "local_mysql",
      "type": "mysql",
      "host": "${MYSQL_HOST}",
      "port": "${MYSQL_PORT}",
      "user": "${MYSQL_USER}",
      "password": "${MYSQL_PASSWORD}"
    }
  ]
}
